<UserControl x:Class="FaceMosaicSharp.Views.FaceEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:FaceMosaicSharp.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             PreviewKeyDown="UserControl_PreviewKeyDown"
             PreviewKeyUp="UserControl_PreviewKeyUp">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

         <ScrollViewer Grid.Row="0" Margin="10,10,10,0" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible" x:Name="ImageScrollViewer">
            <Border Background="Gray" BorderThickness="0">
                <Canvas x:Name="ImageCanvas" 
                        Width="{Binding ActualWidth, ElementName=FrameImageControl}"
                        Height="{Binding ActualHeight, ElementName=FrameImageControl}"
                        MouseDown="ImageCanvas_MouseDown" 
                        MouseMove="ImageCanvas_MouseMove" 
                        MouseUp="ImageCanvas_MouseUp"
                        Background="Transparent">
                    <Image Source="{Binding FrameImage}" Stretch="None" x:Name="FrameImageControl"/>
                    
                    <!-- 正在绘制的区域 -->
                    <Border x:Name="DrawingRect" BorderBrush="Blue" BorderThickness="2" 
                            Background="#400000FF" Visibility="Collapsed"
                            Canvas.Left="0" Canvas.Top="0"/>
                </Canvas>
            </Border>
        </ScrollViewer>

        <Grid Grid.Row="1" Margin="10,5,10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="置信度阈值:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Slider Value="{Binding ConfidenceThreshold}" Minimum="0.01" Maximum="0.99" 
                        TickFrequency="0.01" IsSnapToTickEnabled="True" IsMoveToPointEnabled="True" 
                        Width="200" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding ConfidenceThreshold, StringFormat='{}{0:P0}'}" 
                           VerticalAlignment="Center" Margin="10,0,10,0"/>
                <Button Content="检测" Command="{Binding ReDetectCommand}" Width="100" 
                        Background="Blue" Foreground="White"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="保存" Command="{Binding SaveCommand}" Width="100" Margin="0,0,10,0" Background="Green" Foreground="White"/>
                <Button Content="关闭" Click="CloseButton_Click" Width="100"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
